build
dist / media
node_modules
.DS_Store
.next
next-env.d.ts
*.tsbuildinfo
.vercel

# .env is tracked intentionally (private repo). Still ignore local-only overrides:
.env.local
.env.*.local

# Local SQLite database — tracked intentionally (private repo).
# The write-ahead log / shared-memory files are transient runtime artifacts, keep them ignored:
payload.db-shm
payload.db-wal

# Local database backups — keep out of version control:
payload.db.backup-*

# Payload media uploads — tracked intentionally (private repo, keeps clones self-contained)
# public/media/

public/robots.txt
public/sitemap*.xml


# Playwright
node_modules/
/test-results/
/playwright-report/
/blob-report/
/playwright/.cache/
